#include <bits/stdc++.h>
#define ll long long
#define ld long double
#define arr array
#define sza(x) ((int)x.size())
#define all(a) (a).begin(), (a).end()

using namespace std;

int main(){
    
    ios::sync_with_stdio(false);
    cin.tie(0);

    ll t;
    cin >> t;

    while(t--){
       
    }

    return 0;
}